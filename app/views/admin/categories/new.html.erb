<% content_for :title, "Add New Category - Admin" %>

<!-- Page Header -->
<div class="mb-8">
  <div class="flex items-center justify-between">
    <div>
      <h1 class="text-3xl font-bold text-white mb-2">Add New Category</h1>
      <p class="text-gray-400">Create a new category to organize software</p>
    </div>
    <%= link_to "← Back to Categories", admin_categories_path, 
        class: "text-green-400 hover:text-green-300 transition-colors" %>
  </div>
</div>

<%= render 'form', category: @category %>