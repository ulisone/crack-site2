<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Software Crackz" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Google Fonts %>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Fira+Code:wght@300;400;500;600;700&family=Orbitron:wght@400;700;900&display=swap" rel="stylesheet">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-900 text-white">
    <!-- Header -->
    <header class="bg-gray-800 bg-opacity-95 backdrop-blur border-b border-gray-700 sticky top-0 z-50">
      <div class="container mx-auto px-4 py-4">
        <div class="flex items-center justify-between">
          <h1 class="site-title text-center">
            <%= link_to "Software Crackz", root_path, class: "hover:no-underline" %>
          </h1>
          <nav class="flex space-x-6">
            <%= link_to "Home", root_path, class: "text-gray-300 hover:text-green-400 transition-colors" %>
            <%= link_to "Categories", categories_path, class: "text-gray-300 hover:text-green-400 transition-colors" %>
          </nav>
        </div>
      </div>
    </header>

    <div class="flex min-h-screen">
      <!-- Sidebar -->
      <aside class="category-sidebar w-64 flex-shrink-0">
        <h3 class="text-lg font-semibold mb-4 text-green-400">Categories</h3>
        <nav>
          <%= link_to "All Software", root_path, 
              class: "category-item #{'active' if params[:category_id].blank?}" %>
          <% @categories.each do |category| %>
            <%= link_to root_path(category_id: category.id), 
                class: "category-item #{'active' if params[:category_id] == category.id.to_s}" do %>
              <span class="block"><%= category.name %></span>
              <span class="text-xs text-gray-500">(<%= category.softwares_count %>)</span>
            <% end %>
          <% end %>
        </nav>
      </aside>

      <!-- Content Area -->
      <main class="flex-1 p-6">
        <% if notice %>
          <div class="bg-green-600 text-white p-4 rounded mb-6 animate-pulse">
            <%= notice %>
          </div>
        <% end %>
        
        <% if alert %>
          <div class="bg-red-600 text-white p-4 rounded mb-6 animate-pulse">
            <%= alert %>
          </div>
        <% end %>
        
        <%= yield %>
      </main>
    </div>

    <!-- Footer -->
    <footer class="bg-gray-800 border-t border-gray-700 py-8 mt-12">
      <div class="container mx-auto px-4 text-center text-gray-500">
        <p>&copy; 2024 Software Crackz. For educational purposes only.</p>
        <p class="text-sm mt-2">This site is for educational and research purposes. Always respect software licensing.</p>
      </div>
    </footer>
  </body>
</html>
